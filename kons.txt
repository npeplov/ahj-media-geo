1. Тесты только для ввода GPS координат.
v 2. Сервер не нужен, все хранится в памяти.
v 3. Новые записи добавляются сверху

4. ввод кординат вручную:
  v модальное окно: инпут, отмена, ок
  - валидация координат

При несоответствии формата функция должна генерировать исключение, которое должно влиять на 
валидацию поля (валидацию мы проходили).

- должны быть обе координаты:
if (!this.gui.modalInput.value.match(reg).length === 2) {
  должна генерировать исключение
}

1. elem.validity.customError
2. Метод HTMLSelectElement.setCustomValidity() устанавливает  
специальное сообщение для  выбранного элемента